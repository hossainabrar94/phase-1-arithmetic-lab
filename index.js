/*function multiply(num1, num2){
    if(typeof num1 === "number"){
        if(typeof num2 === 'number'){
            num1 = 31;
            num2 = 2;
            return num1*num2
        }
    }
}
*/
let multiply, num1=31, num2=2, random, mod, num3=19, num4=5, max
multiply = num1*num2;
random = Math.floor(Math.random()*10);
mod = num3%num4;
max = Math.max(1,19,20);